# Rules

- Do not commit or push automatically. Leave changes uncommitted for review; commit/push only when explicitly asked.
- After every push to remote, wait for the Pages build to finish, then verify that the link to every document works on the live site (https://nknysh.github.io/cv/): fetch the landing page and request each linked document URL, confirming HTTP 200. Report any failures.
